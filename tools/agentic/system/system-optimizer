#!/usr/bin/env bash
# sysop - Dynamic Optimization & Hardware Profile Integrator v1.0.1

set -euo pipefail

MYSYS_FILE="$HOME/.config/local-ai/skills/system/mysys.md"
SYSOP_FILE="$HOME/.config/local-ai/skills/system/system-optimizer.md"

echo "# SYSTEM AUTOMATION & OPTIMIZATION PROFILE"
echo ""

if [ -f "$MYSYS_FILE" ]; then
    cat "$MYSYS_FILE"
    echo ""
    echo "---"
    echo ""
fi

if [ -f "$SYSOP_FILE" ]; then
    cat "$SYSOP_FILE"
fi
